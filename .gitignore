# deps
node_modules/
.pnp
.pnp.js

# build
.next/
out/
dist/
build/

# env
.env*.local
.env

# testing
coverage/
playwright-report/
test-results/
.lighthouseci/

# os
.DS_Store
Thumbs.db

# editor
.vscode/
.idea/

# claude / brainstorm artifacts
.claude/
.superpowers/

# scener (large source videos)
scener/

# Frame WebPs are committed (Option A in deploy plan): Railway build
# can't regenerate them — scener/*.mp4 is gitignored (too large) and
# Nixpacks doesn't have ffmpeg. ~131 MB but only updated when source
# videos change. Plan: move to object storage (R2/S3) later.

# logs
*.log
.lighthouseci/
